import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function DashboardAboutPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>O Projekcie</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Platforma do zarządzania lekcjami muzyki MusicDesk.</p>
      </CardContent>
    </Card>
  );
}